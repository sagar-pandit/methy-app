# Jaeger Configuration

Jaeger all-in-one tracing backend for the Methy platform.

Currently runs with default settings via `docker-compose --profile observability up`.

> Custom configuration (sampling strategies, storage backends) will be added in Phase 7.
