{
  "name": "methy-workspace",
  "version": "1.0.0",
  "description": "Methy platform workspace — git hooks, linting, and commit enforcement",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.3",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  }
}
